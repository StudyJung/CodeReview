# CodeReview_20260421_1776776201.md
- "테스트 코드라도 회귀는 남는다 — 현재 범위는 빌드 차단과 nullptr 재유입이 함께 남아 있다."

## 요약
| 항목 | 내용 |
| :--- | :--- |
| **Git Range** | 151efc08 - HEAD(f6e735e) ( Relevant commits 13 / Source files 2 ) |
| **Period** | 2026/04/19 11:41:23 - 2026/04/20 08:25:39 |
| **Model(Effort)** | Codex GPT-5 |
| **Tools** | '/check', `git log`, `CodeReview.py clean-output` |

| 등급 | 건수 | 설명 |
| :--- | :--- | :--- |
| P0:Critical | 0개 | |
| **P1:High** | 3개 | `TEST()` nullptr 재회귀 1건, 현재 빌드 차단 2건 |
| P2:Medium | 2개 | 2차 헤더 체인 1건, dead code 컴파일 차단 1건 |
| P3:Low | 0개 | |
| P4:Minimal | 1개 | 반환 타입 의미 불명 1건 |
| **합계** | 6개 | 테스트/툴 코드 감산 적용 후에도 빌드 불가 상태 |

## P1:High - 2026/04/21

#### [P1:Crash regression][Huns][b41368f][Test.cpp/TEST()/Line:12-14]
[원인] nullptr pT 재역참조 회귀
```cpp
int* pT = nullptr;

printf("T5 %d.\n ", *pT);

printf("T7 %d.\n ", *pT);
```
 <details>
  <summary>상태 : [미결] , 위험 : 81, 횟수 : 1, 추적 : 2026/04/19 - 2026/04/21</summary>
  <br>- 설명 : `TEST()`의 직접 `*pT` 역참조는 `aae2843`와 `17d38f8`에서 일시 제거됐지만 `f625d50`에서 `T5`로 재유입됐고, `b41368f`에서 `T7`까지 추가되어 현재는 동일 nullptr를 두 번 역참조한다. 헤더/식별자 문제를 먼저 정리하면 테스트 실행 즉시 크래시가 재현된다.
  <br><br>- 의견 : `pT`를 유효 객체로 바꾸거나, 단순 출력 목적이면 포인터를 제거하고 값 변수로 치환.
  <br><br>- 기타 : `3136b89`는 `T1`과 `T7` 일부만 제거했고 `T5`는 남겨 root cause를 해소하지 못했다. `TEST()` 상위 호출이 추가되면 즉시 재폭발 가능.
 </details>

#### [P1:Compile blocker][Huns][151efc0][Test.cpp/main()/Line:25]
[원인] 미정의 포인터 pP 사용
```cpp
printf("T3 %d.\n ", *pP);
```
 <details>
  <summary>상태 : [미결] , 위험 : 77, 횟수 : 1, 추적 : 2026/04/19 - 2026/04/21</summary>
  <br>- 설명 : `main()` 진입 함수에서 선언되지 않은 `pP`를 즉시 역참조한다. 현재는 식별자 해석 단계에서 컴파일이 멈추고, 나중에 임시로 `pP`를 선언해도 `pT`/`pM`과 같은 nullptr 패턴으로 재발할 가능성이 높다.
  <br><br>- 의견 : 실제 필요한 변수라면 선언과 유효 초기화를 함께 추가하고, 그렇지 않으면 해당 출력 라인을 제거.
  <br><br>- 기타 : `3136b89`에서 `pM` 직접 크래시는 제거되어 해결 상태지만, `main()` 상위 진입점 기준 현재 남은 차단 이슈는 이 라인이다.
 </details>

#### [P1:Missing header][Huns][151efc0][Test.cpp/global/Line:1]
[원인] Huns.h 헤더 누락
```cpp
#include "Huns.h"
```
 <details>
  <summary>상태 : [미결] , 위험 : 74, 횟수 : 1, 추적 : 2026/04/19 - 2026/04/21</summary>
  <br>- 설명 : `Test.cpp`의 첫 include 대상인 `Huns.h`가 저장소에 존재하지 않는다. 현재 소스는 전처리 단계에서 바로 중단되므로 아래 함수 본문 이슈보다 먼저 빌드를 막는다.
  <br><br>- 의견 : 실제 의도가 `Test.h`라면 include를 복구하고, 별도 헤더가 맞다면 파일을 추가해 선언을 정리.
  <br><br>- 기타 : 이 문제를 바로잡으면 `Test.h` 내부의 `Tests.h` 누락이 다음 차단 요소로 드러난다.
 </details>

## P2:Medium - 2026/04/21

#### [P2:Header chain][Huns][151efc0][Test.h/global/Line:3]
[원인] Tests.h 2차 헤더 누락
```cpp
#include "Tests.h"
```
 <details>
  <summary>상태 : [미결] , 위험 : 63, 횟수 : 1, 추적 : 2026/04/19 - 2026/04/21</summary>
  <br>- 설명 : 저장소에는 `Test.h`만 있고 `Tests.h`는 없다. `Huns.h` 경로를 정리해도 곧바로 이 include 체인에서 다시 빌드가 멈춘다.
  <br><br>- 의견 : 올바른 헤더명으로 수정하거나 불필요한 include라면 제거.
  <br><br>- 기타 : 상위 include 수정만으로는 빌드가 복구되지 않는 연쇄 차단이라 함께 정리해야 한다.
 </details>

#### [P2:Dead code compile][Huns][151efc0][Test.cpp/TEST()/Line:7-10]
[원인] dead code 내 p5 미정의
```cpp
if(  false)
{
	printf("T4 %d.\n ", *p5);
}
```
 <details>
  <summary>상태 : [미결] , 위험 : 58, 횟수 : 1, 추적 : 2026/04/19 - 2026/04/21</summary>
  <br>- 설명 : 분기 자체는 실행되지 않지만 C++ 컴파일러는 블록 내부 식별자를 그대로 해석하므로 `p5` 미정의로 빌드가 실패한다. `if(false)` dead code라 런타임 영향은 낮아 위험도를 감산했다.
  <br><br>- 의견 : dead code 블록을 제거하거나, 유지가 필요하면 최소한 유효한 식별자로 바꿔야 한다.
  <br><br>- 기타 : `main()`의 `pP`와 동일한 미정의 포인터 패턴이다. 해당 함수 전반에 임시 테스트 코드가 누적된 상태로 보인다.
 </details>

## P4:Minimal - 2026/04/21

#### [P4:Return semantics][Huns][151efc0][Test.cpp/TEST()/Line:16]
[원인] int TEST()에서 bool 반환
```cpp
return true;
```
 <details>
  <summary>상태 : [미결] , 위험 : 09, 횟수 : 1, 추적 : 2026/04/19 - 2026/04/21</summary>
  <br>- 설명 : `TEST()`의 반환형은 `int`인데 실제 반환값은 `bool` 상수다. 컴파일은 되지만 성공/실패 규약이 불명확하고, 이후 호출부가 정수 의미를 기대하면 오해를 만든다.
  <br><br>- 의견 : 성공 코드라면 `return 0;`처럼 명시하고, 반환값이 불필요하면 함수 시그니처를 정리.
  <br><br>- 기타 : 현재는 상위 P1/P2 이슈들 때문에 실행 전 단계에서 막히므로 우선순위는 낮다.
 </details>
