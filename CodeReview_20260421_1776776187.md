# CodeReview_20260421_1776776187.md
- "단순함은 신뢰성을 높인다." - Edsger W. Dijkstra

## 요약
| 항목 | 내용 |
| :--- | :--- |
| **Git Range** | 151efc0 - b41368f ( Target Commits 13 ) |
| **Period** | 2026/04/19 11:41:23 - 2026/04/20 08:25:39 |
| **Model(Effort)** | Codex GPT-5 |
| **Tools** | `git log`, `git diff`, `git blame`, `CodeReview.py clean-output` |

| 등급 | 건수 | 설명 |
| :--- | :--- | :--- |
| **P0:Critical** | 2개 | `pT` nullptr 역참조와 회귀 |
| **P1:High** | 3개 | 빌드 차단 심볼/헤더 누락 |
| P2:Medium | 1개 | dead code 내부 미선언 심볼 |
| P3:Low | 1개 | 헤더 위생 저하 |
| [해결] | 2개 | `T4`, `T1` nullptr 역참조 제거 |
| **합계** | 9개 |   |

## P0:Critical - 2026/04/21

#### [P0:Crash regression][Huns][b41368f][test/Test.cpp/TEST()/Line:14]
[원인] T7 nullptr 역참조 재도입
```cpp
	printf("T5 %d.\n ", *pT);

printf("T7 %d.\n ", *pT);
```
[추천] 두 호출부를 한 번에 가드
```cpp
	if (pT != nullptr)
	{
		printf("T5 %d.\n ", *pT);
		printf("T7 %d.\n ", *pT);
	}
```
 <details>
  <summary>상태 : [미결] , 위험 : 99, 횟수 : 1, 추적 : 2026/04/20 - 2026/04/20</summary>
  <br>- 설명 : 커밋 `b41368f`가 커밋 `3136b89`에서 제거된 `T7` 역참조를 다시 추가했다. `pT`는 Line 5에서 `nullptr`로 고정되고 Line 14까지 값이 바뀌지 않으므로 현재 최신 소스에서도 즉시 크래시 후보가 남아 있다.
  <br><br>- 의견 : `T7`만 지우는 임시 조치보다 `TEST()` 내부의 `pT` 사용 구간 전체를 한 번에 가드하거나 유효 포인터를 주입해야 한다. 그렇지 않으면 Line 12 또는 Line 14 중 하나가 다시 남는다.
  <br><br>- 기타 : 같은 함수의 Line 12도 동일 `pT`를 역참조한다. Line 14는 들여쓰기도 깨져 있어 회귀 흔적이 명확하다.
 </details>

#### [P0:Crash nullptr][Huns][b41368f][test/Test.cpp/TEST()/Line:12]
[원인] nullptr 초기화 후 즉시 역참조
```cpp
int* pT = nullptr;
...
	printf("T5 %d.\n ", *pT);
```
[추천] 유효 포인터로 교체 또는 가드
```cpp
	int value = 0;
	int* pT = &value;
	printf("T5 %d.\n ", *pT);
```
 <details>
  <summary>상태 : [미결] , 위험 : 97, 횟수 : 1, 추적 : 2026/04/19 - 2026/04/20</summary>
  <br>- 설명 : `pT`는 `TEST()` 시작 시 `nullptr`로 초기화되고 이후 어떤 분기에서도 재할당되지 않는다. 현재 최신 소스의 Line 12는 항상 잘못된 역참조가 되며, `b41368f`가 같은 블록을 다시 건드리면서 문제 상태를 유지했다.
  <br><br>- 의견 : `TEST()`가 포인터 테스트 코드라면 역참조 자체를 제거하고, 실제 동작 코드라면 유효 객체를 바인딩한 뒤 사용해야 한다. 단순히 Line 14만 제거해도 Line 12 크래시는 그대로 남는다.
  <br><br>- 기타 : 동일 변수에 대한 이전 `T4` 역참조는 `0a08116`에서 제거되어 [해결] 상태다. 현재 살아 있는 런타임 크래시 경로는 `T5`, `T7` 두 곳이다.
 </details>

## P1:High - 2026/04/21

#### [P1:Undefined symbol][Huns][151efc0][test/Test.cpp/main()/Line:25]
[원인] pP 미선언 상태로 역참조
```cpp
	printf("T3 %d.\n ", *pP);
```
 <details>
  <summary>상태 : [미결] , 위험 : 90, 횟수 : 1, 추적 : 2026/04/19 - 2026/04/20</summary>
  <br>- 설명 : 최신 `test/Test.cpp`와 저장소 내 어떤 허용 확장자 파일에도 `pP` 선언이 없다. `main()`의 직접 구문이므로 빌드 단계에서 바로 실패하고 프로그램 진입 자체가 막힌다.
  <br><br>- 의견 : `pP`를 실제 포인터로 선언해 전달하거나 출력 라인을 제거해야 한다. 엔트리 함수 내부라 상위 호출부에서 우회할 방법도 없다.
  <br><br>- 기타 : `main()`의 `pM` 역참조는 `3136b89`에서 제거되어 현재는 남아 있지 않다. 현재 `main()`의 치명적인 잔존 문제는 `*pP` 한 건이다.
 </details>

#### [P1:Include missing][Huns][151efc0][test/Test.cpp/global/Line:1]
[원인] 존재하지 않는 Huns.h 포함
```cpp
#include "Huns.h"
```
 <details>
  <summary>상태 : [미결] , 위험 : 88, 횟수 : 1, 추적 : 2026/04/19 - 2026/04/20</summary>
  <br>- 설명 : 현재 저장소의 허용 확장자 파일은 `test/Test.cpp`, `test/Test.h`뿐이고 `Huns.h`는 존재하지 않는다. 번역 단위가 시작 단계에서 깨지므로 `TEST()`, `main()` 내부 수정만으로는 빌드가 복구되지 않는다.
  <br><br>- 의견 : 실제 의도가 `Test.h` 사용이라면 include 대상을 교체하고, 별도 헤더가 필요하다면 파일을 추가해야 한다. 현재 상태에서는 상위 include 체인이 첫 줄에서 끊긴다.
  <br><br>- 기타 : include를 `Test.h`로 바꿔도 그 안의 `Tests.h` 누락이 이어진다. 헤더 체인을 함께 정리해야 한다.
 </details>

#### [P1:Include chain missing][Huns][151efc0][test/Test.h/global/Line:3]
[원인] Tests.h 누락으로 헤더 체인 단절
```cpp
#include "Tests.h"
```
 <details>
  <summary>상태 : [미결] , 위험 : 86, 횟수 : 1, 추적 : 2026/04/19 - 2026/04/20</summary>
  <br>- 설명 : `test/Test.h`가 다시 존재하지 않는 `Tests.h`를 참조한다. `Test.cpp`의 첫 include를 복구하더라도 다음 단계에서 동일하게 컴파일이 멈춘다.
  <br><br>- 의견 : 실제 필요한 선언을 이 파일로 합치거나 존재하는 헤더로 교체해야 한다. 첫 번째 include 오류만 고치고 끝내면 빌드 복구가 되지 않는다.
  <br><br>- 기타 : 이 항목은 `test/Test.cpp`의 `Huns.h` 누락과 연속된 상위 include 경로 문제다. RV3 기준으로 사이드 이펙트까지 확인한 결과, 두 문제는 각각 별도로 남아 있다.
 </details>

## P2:Medium - 2026/04/21

#### [P2:Undefined symbol][정훈희][f625d50][test/Test.cpp/TEST()/Line:9]
[원인] dead code 내부 p5 미선언
```cpp
	if(  false)
	{
		printf("T4 %d.\n ", *p5);
	}
```
 <details>
  <summary>상태 : [미결] , 위험 : 68, 횟수 : 1, 추적 : 2026/04/19 - 2026/04/20</summary>
  <br>- 설명 : `p5`는 최신 소스 어디에도 선언되지 않았고 `if(false)` 블록 내부라도 컴파일러는 식별자를 검사한다. 런타임 진입 가능성은 낮지만 현재 소스 기준으로는 여전히 빌드 차단 요소다.
  <br><br>- 의견 : dead code 블록을 제거하거나 테스트 의도라면 유효 심볼로 교체해야 한다. RV6에 따라 `if(false)` 자체는 위험도를 감산했지만 컴파일 실패 사실은 남는다.
  <br><br>- 기타 : 이전 `T4`의 `*pT` nullptr 역참조는 제거되었고, 현재 이 블록에 남은 실질 문제는 `*p5` 미선언 하나다.
 </details>

## P3:Low - 2026/04/21

#### [P3:Header hygiene][Huns][151efc0][test/Test.h/global/Line:1-5]
[원인] 헤더에 불필요 include와 namespace 노출
```cpp
#include <iostream>

#include "Tests.h"

using namespace std;
```
 <details>
  <summary>상태 : [미결] , 위험 : 24, 횟수 : 1, 추적 : 2026/04/19 - 2026/04/20</summary>
  <br>- 설명 : `Test.h` 안에서는 스트림 심볼을 직접 사용하지 않는데 `<iostream>`을 포함하고, 동시에 `using namespace std;`를 헤더 전역에 둔다. 헤더가 실제로 연결되면 모든 포함 지점으로 namespace 오염이 전파된다.
  <br><br>- 의견 : 필요한 헤더만 남기고 `using namespace std;`는 소스 파일 또는 국소 범위로 내리는 편이 안전하다. 테스트 코드이므로 RV7 기준으로 중요도는 크게 낮췄다.
  <br><br>- 기타 : 현재는 상위 include 누락 때문에 즉시 드러나지 않지만, 헤더 체인을 복구한 뒤 그대로 남는 후속 품질 문제다.
 </details>
